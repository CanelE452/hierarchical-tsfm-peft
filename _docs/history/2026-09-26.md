# 2026-09-26

## 승인된 TSFM PEFT 자율 개발 착수

- 사용자 전체 계획 승인 후 research/tsfm_peft_development_20260926/ 생성. PLAN.md는 현재 채팅 rollout의 직전 계획 응답 원문, STATUS.md는 재개용 상태.
- 기존 root/main 0602346과 unrelated untracked history/transient 연구/체크포인트 보존. 과거 MASTER의 단발 계약은 새 승인 범위에 확장하지 않음.
- 채널 압축 E/D와 재구성잔차의 저랭크 시간예측기를 구현. frozen Bolt-small, L512/H48, MSE 중심. 표준 LoRA/native loss 및 compression/raw-bypass/선형대조 포함.
- 데이터 전문 agent가 TRAIN-only preprocessing, Bull17 후보·16적격채널 봉인과 노출 감사 수행. 기존 참조 repo 수정 없음. 보호 성능 미열람.
- CPU 데이터6검사와 구조검사, 실제 GPU checkpoint3smoke updates 완료. gradient/동결/공식추론/복원 정합성 통과.
- initial16fit 비교 실행 시작. 숫자는 각 실행 curve/result와 gpu_jobs.json에 저장. 아직 새 방법 선택 근거 아님.

## 최초 기준선 진행 및 평가 검산

- LoRA/COMPRESS 첫 seed의 4fit 완료, 잔차 모델 학습 진행. 최초16fit 전체 또는 주제 선택 완료를 주장하지 않는다.
- verify_results.py에서 저장 예측의 scalar MSE, 최소VAL checkpoint 선택, 실제 표본 schedule hash와 checkpoint hash를 검산했다. 4fit 최대 수치 오차8.4e-16 이하. 별도 독립 재현은 아니다.
- evaluate_development.py는 비순환7원점 시간 블록과 seed별 대응 효과를 사용하며 인공 배열4검사 PASS. 실제 개발 비교는 최초16fit 완료 후 실행한다.
- 보호 데이터 archive hash를 추가로 보존했다. 사전 TRAIN 적격성/ID/분할과 보호 구간 미평가 상태는 유지한다.
- 결측이 있는 Bull 적응 전에 effective batch의 채널별 관측 수로 MSE gradient를 집계해야 함을 확인했다. 완전 관측 Electricity의 현재 학습 결과에는 영향 없다. 실행 중 코드 변경은 보류했다.

## 전체 TRAIN 선형 대조와 첫 seed 완료

- 최초 선형 대조의512창과 신경망의 매epoch 재표집 차이를 확인하여, 같은 PCA/선형 모형/penalty 선택으로 전체TRAIN17853창 대조를 추가했다. CPU9fit 추가·10.44초, 원본 보존. 데이터·신경망·GPU 상한 변경 없음.
- factor-linear VAL MSE0.174000/DEV0.178457로 강화되어 현재 첫seed RESIDUAL VAL0.178411보다 낮다. TSFM의 추가 가치 미확보이며 이후 평가기에 이 대조를 연결했다.
- 첫seed8fit 완료, 두 번째seed 실행 중. 저장된8신경망VAL 및 선택된4선형VAL/DEV 예측을 scalar 계산으로 검산했다. 오차 각각8.4e-16/3.3e-11이하. 독립 재현이 아니다.
- test_evaluation.py의 결측/완전관측 effective-batch gradient 검산을 포함해6개 PASS. 실제 학습 루프의 결측 집계 수정은 현재initial job 종료 후 적용한다.

## 저장 검증 예측의 오차 위치 진단

- TRAIN PCA8 고정공간에서 전체 검증 오차를 공통·직교잔차로 CPU 분해했다. 첫seed RESIDUAL은 factor-linear보다 공통 오차가0.003924 낮고 직교잔차 오차가0.008335 높다. 모델 branch의 인과 분해나 backbone 기여 증명이 아니며 다음 개발 진단의 단서로만 사용한다.
- 검증 앞기간에서 차이가 크고 뒤기간 총MSE는 비슷했다. 전체·두기간 모두 분해 identity/직교성/원점/hash/저장지표 일치 검산을 추가했다. Electricity 저장예측만 읽고 새 모델 예측이나 Bull 점수는 생성하지 않았다.
- LoRA 두 번째seed까지 최초10fit 완료; 완료결과 검산도10fit으로 갱신했다. GPU 최초 비교는 계속 진행 중이며 아직 최종 개발 판단이 아니다.

## 최초16fit 평가와 실행 정합성 보정

- 최초16fit 완료·저장 검산 PASS. 개발 평균MSE는 RESIDUAL0.176130/RAW0.203863/COMPRESS0.259022/factor-linear0.178457/F00.164537/LoRA0.161096. factor 대비 이득1.30%는 기간별부호가 다르고 조건부block구간이0을 포함하여 TSFM 추가 가치 미확보.
- LoRA 객체의 순환 참조가192MB를 유지하는 현상을 실제 로딩/삭제로 확인했다. 최초 메모리 peak의 모델간 비교를 무효로 표시하고 GC 후 비용만 재검사했다. 동일선택의 저장10NPZ와 모든 정확도·효과 수치가 원본과 정확히 같았다.
- 결측 effective-batch MSE 집계를 run.backward_batch에 통합했다. CPU8검사 PASS. 실체크포인트4군×완전/인공결측에서 micro1/4 gradient상대오차최대5.84e-7, optimizer update0. 후속fit은 동일effective batch4의 micro4 실행을 명시한다.
- 학습상한에서 개선 중인6설정을 같은초기화로 최대120epoch까지 별도 재학습할 근거와 specs를 저장했다. 원본16fit은 보존하며, 재학습 시간·attempt도 기존24시간/48회에 포함한다. 구조수정0/2, 보호평가 미실행 유지.

## 학습 연장·용량 진단·기록 저장 오류 복구

- 연장2fit 완료: LoRA고LR seed1은46epoch/선택40, RESIDUAL저LR seed1은63epoch/선택57에서 정체 종료했다. 완료18fit 저장검산 PASS. 나머지 군선택·DEV 비교는 아직 미완료다.
- RAW저LR seed1은epoch30 검증 중 JSON 원장의 atomic replace WinError5로 실패했다. 원본attempt/곡선/error를 보존하고, 최대1.55초 bounded retry를 추가했다. 이어서 한글 실패메시지를cp949로 읽던 오류도UTF-8 명시로 수정했다. CPU12검사 PASS, retry의공통30검증시점은원본과exact equal. 실패도48attempt/24시간에포함한다.
- CPU reduced-rank ridge 진단4개(8.18초): fullTRAIN/PCA8/common map/penalty0.001을 고정하고 잔차map rank8/16/32/48의VAL MSE는0.187223/0.178386/0.174407/0.174000. 양기간에서rank8손실확인, rank48원본재현·인공rank/목적함수검산PASS. 이는신경망용량수정의단서이며실제TSFM방법개선증거는아니다.
- 기록파일오류복구후4개미완료fit를재개했고,완료gate·CPU검산을거친뒤extended DEV평가를수행하도록연결했다. 필요시잔차rank8→32만수정하는동일용량RAW포함8fit설정을준비했으며아직실행하지않았다. 보호구간은미평가다.

## 학습량 보완 완료와 첫 구조 수정 실행

- 복구4fit와 전체연장6fit 모두정체종료했다. 22완료fit/실패1attempt 보존. 저장결과검산 및extended DEV평가완료. RESIDUAL MSE0.176131, RAW0.203863, factor-linear0.178457. factor대비1.303%이득의조건부block95는[-1.816%,3.931%]로TSFM방법추가가치불확실성이남았다.
- 선택된rank8설정의학습상한문제를보완한뒤에도위결론이유지되어, CPU잔차용량진단과함께첫구조수정rank32를실행했다. latent8/E/D/backbone/손실/표본규칙은유지하고 RESIDUAL/RAW×2LR×2seed의8fit를같은기회로비교한다. 기존LoRA/COMPRESS는재사용하며 VAL-only선택코호트를실행전에고정했다. 구조수정1/2.
- 보호평가설정·해시검증모듈과합성테스트를준비했다. 기존평가/기록저장검사를합친CPU57검사PASS. 현재run.py에는아직연결하지않았고final_seal/Bull적응/보호평가는실행하지않았다.

## rank32 첫 seed 결과와 결측 기준선 준비

- 첫seed의rank32 RESIDUAL/RAW 4fit가모두정체종료했다. 고LR VAL MSE는제안0.167624/RAW0.178235/강화factor0.174000이다. 아직두번째seed와DEV평가전부분결과이므로주제확보를선언하지않는다. 총26완료fit 저장검산PASS.
- masked_linear.py/test_masked_linear.py에결측target의horizon별affine ridge와PCA전체벡터선택을구현했다. 별도정상방정식참조해·미관측target변조반례포함합성CPU6검사PASS. 기존선형대조와같은수치floor를사용한다. 관측수로가중하는shared목적및factor의부분관측벡터제외제약을STATUS에명시했다. 실제자료fit·Bull적응·보호평가없음.

## 첫 구조 수정 비교 완료와 압축 차원 대조

- rank32 8fit와DEV평가완료. 총30완료fit/실패1attempt, 저장검산PASS. 제안방법DEV MSE0.165850은같은용량RAW보다8.11%,강화factor보다7.06%낮고두seed/두기간에이득이남았다. 미압축F0/LoRA보다는0.80%/2.95%높은오차. 재사용개발자료의결과이며보호확증은아니다.
- PLAN의단순대안인COMPRESS latent16×2LR×2seed 4fit를시작했다. 모듈추가없이압축차원만늘리고같은학습기회를제공한다. 평가기는기존rank32예측을hash/origin/지표검산후재사용하여추가선택된2모델만새예측한다.
- run.py의약한seal존재검사를배열로드전전체spec/artifact/최초receipt검사로교체했다. evaluate_protected.py는고정전체cohort/두기간을한번평가하고원본실패/부분결과를남긴다. 총103합성CPU검사PASS; 실제보호seal/적응/평가는아직없다.
