# 2026-09-27

## 승인된 채널 압축 잔차 PEFT 후속 개발 v2 착수

- 기준 상태: main/원격 main `ef59f1e921bc70c93b919e095c4d3d6f048ca319`. 사용자가 v2 계획 전체와 구현·진단·학습·대조·비용 측정·관련 main 게시를 승인했다.
- 신규 폴더 `research/tsfm_peft_followup_v2_20260926/`에 직전 계획 응답 원문을 PLAN.md로 보존했다. SHA256 `9a93a4cfb869a4e05bbfbf3d93f4f4611c2c1b4b4cbee7ca79ff1ede4645436f`. v1 추적 파일191개의 원래 hash를 provenance.json에 기록했다.
- 변경 전: CURRENT 공동 E/D 적응만 있고 Bull E1에서 LoRA 대비 큰 손해. 변경 후: v2 독립 실행기에 FIXED_ED/SLOW_ED 및 대응 RAW 경로, 고정 E/D까지 포함한 저장·복원, 실제 fit 내 gradient/갱신/동결/복원 검사 추가. 기존 v1 코드·봉인·결과·캐시는 수정하지 않는다.
- 승인 계약: 신규20attempt 또는 GPU 점유4시간 중 먼저 도달하는 상한, 신규10GiB. TRAIN 업데이트/VAL 선택, Electricity DEV와 Bull E1/E2는 노출된 개발 평가. 새 확인 자료는 미확보.
- 최소 진단 완료: CURRENT 네 선택 모델의 VAL 예측 replay 최대오차0, GPU26.895648초/업데이트0. Bull E1 추가 오차는 두 seed 모두 선택 decoder 공간 바깥에 주로 분포. 원인 확정이 아니라 첫 E/D 비교 이후 후속 선택의 단서다.
- 첫 신규8fit 시작: 고정·느린 E/D × Electricity/Bull ×2seed. 동일 rank32/초기값/표본 규칙/최대120epoch. 실제 실행·예산·판단은 신규 STATUS와 각 실행 원장에 계속 기록한다.

## 후속 개발 비교 완료

- 신규20fit 완료, fit실패0: FIXED/SLOW RES8, 같은 RAW8, 사전 결정한 Bull FIXED K4→8 RES/RAW4. 전체 VAL·원점 schedule·초기값·동결·갱신·checkpoint 복원 검산 PASS. 최대120epoch에 도달한 실행은 없었다.
- 선택은 Electricity CURRENT K8, Bull FIXED K4이며 RAW는 두 자료 CURRENT. Bull E1은 기존0.267849→0.210365(21.46% 감소), E2는1.156667→1.246580(7.77% 증가). 같은 Bull FIXED K4 RAW 대비 E1은60.60% 감소, E2는6.48% 감소이나 후자는 불확실성이 크다.
- Bull K8은 개발 E1/E2가0.202656/1.239798로 조금 낮지만 VAL에서는 불리해 주력으로 바꾸지 않았다. K4/K8의 G 앞층 초기 난수값 차이를 확인해 순수 차원 인과 절제로 부르지 않는다. 같은 K의 RES/RAW는 실제 초기 tensor 전체가 일치한다.
- 보고서 초안의 RAW 경로와 epoch0 표현은 실제 model.py와 result를 대조해 수정했다. RAW는 공통 `D(F0(E(X)))` 경로에 `G(X)`를 더하며, K8 RAW는6epoch 학습 후 epoch0 checkpoint가 선택된 것이다.
- 비용 첫 시도는 모델 해제 후 cuBLAS 작업공간9.125MiB 잔존으로 검사 중단. 최소 재현으로 workspace 해제 후0을 확인하고 정리 절차만 수정했다. 원본 attempt/partial과 diff를 보존하며 `neural_cost_corrected`로 전체 조건을 다시 측정한다. 학습·선택 결과 변경 없음.
- README에 v2 결과 색인을 추가했다. 원자료·가중치·예측 배열과 무관한 기존 미추적 파일은 게시하지 않는다. 최종 비용·예산·게시 반영은 아래에 후속 기록한다.

## 최종 비용·검산·판정

- corrected 비용156행 완료. 병합 LoRA4checkpoint 정합 PASS,모델 교체 allocator0. Electricity 선택RES/LoRA batch4처리량140.334/119.910origins/s, Bull140.529/148.828. Electricity 이점은 RAW에도 있어 압축 경로 공통 효과다. Bull F0 block latency11.483→25.787→27.160ms 등 변동을 발견해 작은 속도 차이의 확정적 우열은 주장하지 않는다.
- CPU 최종 검산에서68예측행·40seed평균의 최대오차5.385e-14,비용요약오차0. checker의 최초 grouping 오류는 원본 final_artifact_checks.json에 보존,수정 후 retry1 PASS. 실험 결과 변경 없음.
- 신규 실자료20fit, GPU 총3489.4492초(58.157분)/4시간, 활성 연구 job0. 새캐시30,423,573bytes. 최종 판정은 Bull E1 부분 진전/E2 손해, 논문 주력 확대와 배포 추가 가치 미확보·보류. 새 독립 확증 없음. 다음 단일 결정은 같은 구조에서 F0만 학습형 선형으로 바꾸는 별도 회차의 대조 여부다.
- 게시 전 PLAN 문구 재확인에서 별도 optimizer 검사도 attempt에 포함해야 하는데 CPU 합성 검사2회(각2step)를 제외한 계상 오류 발견. 따라서 22/20attempt로 상한 초과. 원본 기록을 유지하고 TOPIC/STATUS/README에 정정, 사용자에게 고지했다. GPU시간·실자료20fit 사실은 변하지 않으나 승인된 예외로 재해석하지 않는다. 추가 실험 없음.
- 관련 v2코드·JSON·그림·보고서와 README/이력만 게시 대상. 승인 PLAN과 v1 191개 추적hash는 그대로이며 비밀정보·rawarray·weight는 제외했다.

## 게시 완료

- 관련138파일을 개별 staging해 `e025d70ab53a55e0e2a052809fb450206eaab1e8`로 commit하고 `CanelE452/hierarchical-tsfm-peft` origin/main push 후 실제 원격 SHA 일치를 확인했다. 현재 상태 기록은 뒤따르는 일반 commit에 포함한다.
- 보존용 unified patch의 빈 context행에만 공백검사의 blank-at-eol/EOF 예외를 적용했으며 일반 코드·문서 검사는 통과했다. hook 우회/force/이력 재작성 없음. 무관한 미추적4항목과 v1 원본은 유지했다.
